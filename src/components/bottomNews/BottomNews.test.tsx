import React from 'react';
import BottomNews from './BottomNews';
import { render } from '@testing-library/react';

describe('<BottomNews />', () => {
  it('should render without crashing', () => {
    // const { debug } = render(<BottomNews />);
    // debug();
  });
});
